# Fedora RT Kernel Podman 

A Dockerfile to build a Kernel with Realtime Patches applied for Fedora.

Currently tested for :

- Fedora 39
- x86_64 architectures

## Requirements

```bash
sudo dnf install -y podman make
```

## Usage

```bash
make
```
